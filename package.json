{
  "name": "blueprint",
  "version": "0.1.0",
  "description": "Import a CAD drawing (DXF/SVG/PDF) — or photograph an object — read its dimensions, and extrude it into a printable solid. Forked from 2.5D.",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "npx serve .",
    "build": "node build.mjs",
    "test": "node test/e2e.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "playwright-core": "^1.61.1"
  }
}
